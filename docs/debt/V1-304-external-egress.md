# Débitos externos — V1-304

Não bloqueiam desenvolvimento; bloqueiam promoção ao ambiente correspondente:

1. Segurança/Privacidade aprovar regras e padrões reais, incluindo falsos positivos e negativos por domínio.
2. Definir identificadores corporativos adicionais e política PII `MASK`/`BLOCK` por tenant.
3. Validar termos de retenção, treinamento, região e direitos de dados de cada provider.
4. Integrar DLP/scanner corporativo em logs, traces, métricas, rede, memória e artefatos.
5. Definir owners, aprovadores, alertas e cerimônia de revisão/revogação das exceções.
6. Executar red-team com dados exclusivamente sintéticos antes de liberar egress externo.

Nenhuma regra corporativa, exceção, provider ou classificação externa é fixada nesta issue.
