import assert from "node:assert/strict";import {test} from "node:test";import {encryptMemoryExport,selectMemoryForGateway} from "../dist/main/main/domain/memory-policy.js";
test("local memory never crosses gateway selection",()=>{const items=[{itemId:"local",scope:"LOCAL_ONLY",content:"CANARY_LOCAL"},{itemId:"eligible",scope:"GATEWAY_ELIGIBLE",content:"allowed"}];const result=selectMemoryForGateway(items,["local","eligible"],true);assert.deepEqual(result,[{itemId:"eligible",content:"allowed"}]);assert.equal(JSON.stringify(result).includes("CANARY_LOCAL"),false);assert.deepEqual(selectMemoryForGateway(items,["eligible"],false),[]);});
test("export is encrypted and requires a strong one-time password",()=>{assert.throws(()=>encryptMemoryExport([],"short",()=>"x"),/MEMORY_EXPORT_PASSWORD_WEAK/);const result=encryptMemoryExport([{content:"CANARY"}],"long-password-123",value=>Buffer.from(value).toString("base64"));assert.equal(JSON.stringify(result).includes("CANARY"),false);});
