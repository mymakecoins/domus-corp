import assert from 'node:assert/strict';import {test} from 'node:test';import {createDesktopSessionProvider} from '../dist/main/main/application/session-provider.js';
test('desktop session remains in secure store and expires fail closed',async()=>{let raw;const store={available:()=>true,async get(){return raw;},async set(_ref,value){raw=value;},async delete(){raw=undefined;}};const clock={now:()=>new Date('2026-08-09T12:00:00Z')},provider=createDesktopSessionProvider(store,clock),session={accessToken:'CANARY_ACCESS_TOKEN',expiresAt:'2026-08-09T13:00:00Z',userId:'u',deviceId:'d',sessionId:'s'};await provider.save(session);assert.equal((await provider.current()).accessToken,'CANARY_ACCESS_TOKEN');clock.now=()=>new Date('2026-08-09T14:00:00Z');await assert.rejects(()=>provider.current(),/CHAT_SESSION_INVALID/);});
test('unavailable secure store denies session',async()=>{const provider=createDesktopSessionProvider({available:()=>false,async get(){},async set(){},async delete(){}},{now:()=>new Date()});await assert.rejects(()=>provider.current(),/CHAT_SESSION_UNAVAILABLE/);});
