import {validateHealth} from "./security.js";export async function fetchHealth(origin:string,fetcher:typeof fetch=fetch){const target=new URL("/health",origin);if(target.protocol!=="https:"&&!(["127.0.0.1","localhost"].includes(target.hostname)&&target.protocol==="http:"))throw new Error("CONTROL_PLANE_UNAVAILABLE");let response:Response;try{response=await fetcher(target,{redirect:"error",signal:AbortSignal.timeout(5000),headers:{accept:"application/json"}});}catch{throw new Error("CONTROL_PLANE_UNAVAILABLE");}if(!response.ok)throw new Error("CONTROL_PLANE_UNAVAILABLE");try{return validateHealth(await response.json());}catch{throw new Error("CONTROL_PLANE_UNAVAILABLE");}}
