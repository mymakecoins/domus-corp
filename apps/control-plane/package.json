{
  "name": "@domus/control-plane",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "test": "node --test 'test/**/*.test.mjs'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@redis/client": "6.2.0",
    "fastify": "5.8.5",
    "jose": "6.2.8",
    "pg": "8.22.0"
  },
  "devDependencies": {
    "@types/node": "22.18.0",
    "@types/pg": "8.21.0"
  }
}
