import {createAuditAccessEvent} from "../../domain/audit/audit-event.js";export type AuditQuery=Readonly<{tenantId:string;workspaceId?:string;actorId:string;purpose:string;from:string;to:string;limit:number;accessEventId:string}>;export async function queryAudit(dependencies:Readonly<{repository:{queryWithAccessAudit(input:AuditQuery):Promise<readonly unknown[]>}}>,query:AuditQuery){if(!Number.isInteger(query.limit)||query.limit<1||query.limit>10000)throw new Error("AUDIT_LIMIT_INVALID");createAuditAccessEvent({...query,resultCount:0,occurredAt:new Date().toISOString()});try{return await dependencies.repository.queryWithAccessAudit(query);}catch{throw new Error("AUDIT_DEPENDENCY_UNAVAILABLE");}}
