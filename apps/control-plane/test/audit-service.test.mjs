import assert from "node:assert/strict";import {test} from "node:test";import {queryAudit} from "../dist/application/audit/query-audit.js";
test("administrative read records purpose before returning rows",async()=>{const calls=[];const rows=await queryAudit({repository:{async queryWithAccessAudit(input){calls.push(input);return [{eventId:"event"}];}}},{tenantId:"tenant",workspaceId:"workspace",actorId:"admin",purpose:"security investigation",from:"2026-08-01T00:00:00Z",to:"2026-08-02T00:00:00Z",limit:100,accessEventId:"access"});assert.equal(rows.length,1);assert.equal(calls[0].purpose,"security investigation");});
test("audit dependency failure blocks administrative read",async()=>{await assert.rejects(()=>queryAudit({repository:{async queryWithAccessAudit(){throw new Error("offline");}}},{tenantId:"tenant",workspaceId:"workspace",actorId:"admin",purpose:"incident",from:"2026-08-01T00:00:00Z",to:"2026-08-02T00:00:00Z",limit:100,accessEventId:"access"}),/AUDIT_DEPENDENCY_UNAVAILABLE/);});
